# 비상로지스 (BisangLogis) — 제품 요구사항 정의서 (PRD)

> ## 🤖 코딩 에이전트 실행 요청문
>
> 이 문서는 웹 애플리케이션 **비상로지스**의 개발 명세서입니다.
> 아래 지시에 따라 개발을 진행해 주세요.
>
> 1. 이 문서 전체를 먼저 읽고, `6. 개발 작업 목록`의 **Phase 순서대로** 구현하십시오.
> 2. `3. 기술 스택`에 명시된 스택을 임의로 변경하지 마십시오.
> 3. `5. 데이터 모델`의 스키마를 그대로 사용하십시오. 필드명·타입을 임의로 바꾸지 마십시오.
> 4. **금액 관련 로직은 `4.3 금액·정확성 규칙`을 반드시 준수하십시오.** 이 프로젝트에서 가장 중요한 요건입니다.
> 5. 각 Phase 완료 시 동작하는 상태로 커밋하고, 다음 Phase로 넘어가기 전에 진행 상황을 보고하십시오.
> 6. 불명확한 요구사항이 있으면 임의 판단하지 말고 질문하십시오.
> 7. 환경변수(Supabase 키, 팝빌 API 키)는 `.env.local`로 분리하고, `.env.example`을 함께 생성하십시오.

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| **프로젝트명** | 비상로지스 (BisangLogis) |
| **한 줄 정의** | 개인 운송사업자를 위한 배차·매출·지출·세금계산서 통합 관리 웹앱 |
| **사용자** | 관리자 1인 (서비스 운영자 본인 전용, 다중 사용자 미지원) |
| **사용 환경** | 반응형 웹 — PC(사무실) + 모바일(현장) 모두 사용 |
| **개발 목표** | 엑셀로 분산 관리하던 운송 업무 데이터를 하나의 시스템에서 정확하게 기록·집계·발행 |

### 1.1 해결하려는 문제

현재 운송·배차 업무와 관련된 아래 정보가 엑셀 파일로 흩어져 관리되고 있어, 월말 정산과 세무 처리 시 누락과 오류가 발생한다.

- 날짜별 운송 매출 및 입금 여부 추적
- 항목별 지출(차량비·생활비·고정비·세금) 집계
- 거래처별 배차 이력 및 수수료 관리
- 세금계산서 발행 이력 및 매입 계산서 조회

### 1.2 핵심 가치

1. **배차 1회 입력 → 매출 자동 반영** (이중 입력 제거)
2. **미입금 건 즉시 식별** (대시보드 및 목록 배지)
3. **월별 매출·지출·순이익 자동 집계**
4. **세무 자료 엑셀 원클릭 추출**

---

## 2. 사용자 시나리오

### 2.1 현장 시나리오 (모바일)

1. 배차 완료 후 휴대폰으로 접속 → 로그인
2. `배차 등록` 진입 → 거래처명 2~3글자 입력 → 자동완성 선택
3. 상·하차지, 파렛수량, 차량(자동완성), 합계 운임, 수수료 입력
4. 저장 → **매출 데이터로 자동 생성**, 입금여부는 `미입금` 상태로 시작

### 2.2 사무 시나리오 (PC)

1. 대시보드에서 이번 달 매출·지출·순이익 및 미입금 건수 확인
2. 입금 확인된 건을 `입금완료`로 일괄 변경 (입금일 입력)
3. 지출 발생 시 `지출 관리`에서 카테고리 선택 후 등록
4. 월말: 세금계산서 발행(수동 기록 또는 팝빌 발행), 매입 계산서 조회
5. 세무사 전달용 엑셀 다운로드

---

## 3. 기술 스택

| 구분 | 기술 | 비고 |
|---|---|---|
| 프레임워크 | **Next.js 15 (App Router)** | React Server Components 활용 |
| 언어 | **TypeScript** (strict mode) | 금액 로직 타입 안정성 확보 |
| 데이터베이스 | **Supabase (PostgreSQL)** | 무료 티어 |
| 인증 | **Supabase Auth** (Email + Password) | 단일 관리자 계정 |
| ORM / 쿼리 | **Supabase JS Client** (`@supabase/ssr`) | RLS 적용 |
| 스타일 | **Tailwind CSS** + **shadcn/ui** | 표·폼 중심 관리자 UI |
| 폼 검증 | **react-hook-form** + **zod** | 서버·클라이언트 양측 검증 |
| 날짜 처리 | **date-fns** (`ko` locale) | 타임존: `Asia/Seoul` |
| 엑셀 입·출력 | **SheetJS (xlsx)** | 내보내기 + 기존 데이터 가져오기 |
| 세금계산서 | **팝빌(Popbill) API** | 테스트베드 우선 연동 |
| 배포 | **Vercel** | |

### 3.1 필수 제약

- 금액 연산에 `float` / `number` 나눗셈 결과를 그대로 저장하지 말 것. **정수(원) 단위**로만 저장.
- 모든 날짜는 DB에 `date` 또는 `timestamptz`로 저장하고, 화면 표시 시 `Asia/Seoul` 기준으로 변환.
- Supabase RLS를 활성화하여, 인증된 소유자만 자신의 데이터에 접근 가능하게 할 것.

---

## 4. 공통 규칙 (반드시 준수)

### 4.1 인증

- 이메일 + 비밀번호 로그인. 회원가입 화면은 **만들지 않음** (계정은 Supabase 콘솔에서 수동 생성)
- 미인증 사용자는 모든 경로에서 `/login`으로 리다이렉트 (middleware 처리)

### 4.2 삭제 정책 — 소프트 삭제

- 모든 주요 테이블은 `deleted_at timestamptz NULL` 컬럼을 가진다.
- 삭제 요청 시 실제 `DELETE` 하지 않고 `deleted_at`에 시각을 기록한다.
- 기본 조회는 `deleted_at IS NULL` 조건을 항상 포함한다.
- `휴지통` 화면에서 30일 내 복구 가능하게 한다.

### 4.3 금액·정확성 규칙 ★최우선★

> 이 프로젝트의 데이터는 **전자장부 수준의 정확성**이 요구된다. 아래 규칙은 예외 없이 적용한다.

1. **저장 단위**: 모든 금액은 `bigint` (원 단위 정수). 소수점 저장 금지.
2. **부가세 자동 분리** (합계금액 입력 방식)
   - 입력값: `total_amount` (합계금액, 부가세 포함)
   - `supply_amount = round(total_amount / 1.1)` — **반올림(round-half-up)**
   - `vat_amount = total_amount - supply_amount`
   - **검증**: `supply_amount + vat_amount === total_amount` 를 저장 전 반드시 assert. 불일치 시 저장 거부.
   - 부가세 미포함(면세) 건은 `is_vat_exempt = true`로 두고 `vat_amount = 0`, `supply_amount = total_amount`.
3. **수수료**: 건당 정액 직접 입력 (`fee_amount`). 자동 계산 금지.
4. **순수익 계산**: `net_amount = supply_amount - fee_amount` (공급가액 기준)
5. **입력 검증 (zod)**
   - 금액 필드: 0 이상 정수, 최대 99,999,999,999
   - 필수값: 운송일자, 거래처, 상차지, 하차지, 합계운임
   - 날짜: 미래 일자 입력 시 경고 표시 (차단은 아님)
6. **중복 입력 방지**: `운송일자 + 거래처 + 상차지 + 하차지 + 차량번호 + 합계운임`이 모두 동일한 건이 이미 존재하면 저장 전 "중복 가능성 있는 건이 있습니다" 확인 모달을 띄운다.
7. **변경 이력 로그**: 모든 생성·수정·삭제·복구 작업을 `audit_logs` 테이블에 기록한다. (대상 테이블, 대상 ID, 작업 종류, 변경 전/후 JSON, 시각)
8. **월별 검산**: 대시보드 및 월별 리포트에서 `개별 건 합계 === 표시된 월 합계` 를 검증하고, 불일치 시 화면에 경고 배너를 표시한다.
9. **트랜잭션**: 배차 등록 → 매출 자동 생성은 **하나의 트랜잭션**으로 처리한다. 부분 실패 시 전체 롤백. (Supabase RPC / Postgres function 사용)

### 4.4 UI 표시 규칙

- 금액: 3자리 콤마, **우측 정렬**, 단위 `원` 표기
- 미입금: 빨간색 배지 / 입금완료: 회색 배지
- 목록 기본 정렬: 운송일자 내림차순
- 목록 페이지네이션: 50건 단위
- 모바일: 표 대신 카드 리스트로 전환 (`md` 브레이크포인트 기준)

---

## 5. 데이터 모델

> Supabase(PostgreSQL) 기준. 모든 테이블에 `id uuid PK default gen_random_uuid()`, `created_at timestamptz default now()`, `updated_at timestamptz`, `deleted_at timestamptz NULL` 포함.

### 5.1 `clients` — 거래처

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `name` | text NOT NULL | 사업자명 |
| `biz_no` | text | 사업자등록번호 (10자리, 하이픈 제거 저장) |
| `ceo_name` | text | 대표자명 |
| `address` | text | 주소 |
| `biz_type` | text | 업태 |
| `biz_item` | text | 종목 |
| `contact_name` | text | 담당자명 |
| `contact_phone` | text | 담당자 연락처 |
| `contact_email` | text | 세금계산서 수신 이메일 |
| `memo` | text | 비고 |

### 5.2 `vehicles` — 차량·기사

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `plate_no` | text NOT NULL UNIQUE | 차량번호 |
| `driver_name` | text NOT NULL | 기사명 |
| `driver_phone` | text | 기사 연락처 |
| `carrier_name` | text | 소속 운수사 |
| `vehicle_type` | text | 차종/톤수 |
| `memo` | text | 비고 |

### 5.3 `dispatches` — 배차 (매출과 1:1 연동)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `dispatch_date` | date NOT NULL | 운송일자 |
| `client_id` | uuid FK → clients | 거래처 |
| `origin` | text NOT NULL | 상차지 |
| `destination` | text NOT NULL | 하차지 |
| `pallet_count` | integer | 파렛 수량 |
| `vehicle_id` | uuid FK → vehicles NULL | 차량 (등록차량 선택 시) |
| `plate_no_snapshot` | text | 차량번호 (입력 당시 값 보존) |
| `driver_name_snapshot` | text | 기사명 (보존) |
| `driver_phone_snapshot` | text | 기사 연락처 (보존) |
| `carrier_name` | text | 운수사 |
| `dispatcher_name` | text | 배차자 |
| `total_amount` | bigint NOT NULL | 합계 운임 (부가세 포함) |
| `supply_amount` | bigint NOT NULL | 공급가액 (자동 계산) |
| `vat_amount` | bigint NOT NULL | 부가세 (자동 계산) |
| `is_vat_exempt` | boolean default false | 면세 여부 |
| `fee_amount` | bigint default 0 | 수수료 (정액) |
| `payment_method` | text NOT NULL | `TAX_INVOICE` / `TRANSFER` / `CASH` |
| `payment_status` | text NOT NULL default `'UNPAID'` | `UNPAID` / `PAID` |
| `paid_at` | date NULL | 입금일 |
| `memo` | text | 비고 |

> **`*_snapshot` 필드 존재 이유**: 차량 마스터 정보가 나중에 수정되어도 과거 배차 기록의 내용이 바뀌지 않도록 입력 당시 값을 그대로 보존한다. (정확성 요건)

### 5.4 `expenses` — 지출

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `expense_date` | date NOT NULL | 지출일자 |
| `category_major` | text NOT NULL | 대분류 |
| `category_minor` | text NOT NULL | 소분류 |
| `amount` | bigint NOT NULL | 지출 금액 |
| `payment_method` | text | `CARD` / `TRANSFER` / `CASH` |
| `vendor` | text | 거래처/상호 |
| `has_tax_invoice` | boolean default false | 매입 증빙(계산서) 수취 여부 |
| `memo` | text | 비고 |

#### 지출 카테고리 정의 (상수로 관리)

| 대분류 | 소분류 |
|---|---|
| **차량** | 주유비, 하이패스, 정비, 소모품 |
| **생활** | 식비, 편의점, 숙박비, 병원/약국, 우편 |
| **고정비** | 법인넘버관리비, 보험료 |
| **금융·세금** | 대출, 부가가치세, 종합소득세, 지방세, 과태료, 용차비 |

> 카테고리는 코드 상수(`lib/constants/expense-categories.ts`)로 관리하되, 추후 확장 가능하도록 별도 `expense_categories` 테이블로 분리하는 구조도 허용.

### 5.5 `tax_invoices` — 세금계산서 (발행/매출)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `issue_date` | date NOT NULL | 작성일자 |
| `client_id` | uuid FK → clients | 공급받는자 |
| `mode` | text NOT NULL | `MANUAL`(수동 기록) / `POPBILL`(API 발행) |
| `status` | text NOT NULL | `RECORDED` / `ISSUED` / `SENT` / `FAILED` / `CANCELLED` |
| `supply_amount` | bigint NOT NULL | 공급가액 |
| `vat_amount` | bigint NOT NULL | 부가세 |
| `total_amount` | bigint NOT NULL | 합계금액 |
| `item_name` | text | 품목 (기본값: `운송용역`) |
| `popbill_mgt_key` | text NULL | 팝빌 문서관리번호 |
| `popbill_nts_confirm_num` | text NULL | 국세청 승인번호 |
| `popbill_error_message` | text NULL | 발행 실패 메시지 |
| `memo` | text | 비고 |

### 5.6 `tax_invoice_dispatches` — 계산서-배차 연결 (N:M)

| 컬럼 | 타입 |
|---|---|
| `tax_invoice_id` | uuid FK → tax_invoices |
| `dispatch_id` | uuid FK → dispatches |

> 한 장의 계산서에 여러 배차 건을 묶어 청구하는 경우를 지원한다.

### 5.7 `purchase_invoices` — 매입 계산서 (조회 결과 저장)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `issue_date` | date | 작성일자 |
| `supplier_name` | text | 공급자 상호 |
| `supplier_biz_no` | text | 공급자 사업자번호 |
| `supply_amount` | bigint | 공급가액 |
| `vat_amount` | bigint | 부가세 |
| `total_amount` | bigint | 합계금액 |
| `nts_confirm_num` | text | 국세청 승인번호 |
| `source` | text | `POPBILL` / `MANUAL` |

### 5.8 `audit_logs` — 변경 이력

| 컬럼 | 타입 |
|---|---|
| `table_name` | text NOT NULL |
| `record_id` | uuid NOT NULL |
| `action` | text NOT NULL (`CREATE`/`UPDATE`/`DELETE`/`RESTORE`) |
| `before_data` | jsonb NULL |
| `after_data` | jsonb NULL |
| `actor_id` | uuid |
| `created_at` | timestamptz default now() |

### 5.9 `settings` — 내 사업자 정보 / 연동 설정 (단일 행)

| 컬럼 | 타입 | 설명 |
|---|---|---|
| `my_biz_name` | text | 내 사업자명 |
| `my_biz_no` | text | 내 사업자등록번호 |
| `my_ceo_name` | text | 대표자명 |
| `my_address` | text | 주소 |
| `my_biz_type` | text | 업태 |
| `my_biz_item` | text | 종목 |
| `popbill_enabled` | boolean default false | 팝빌 연동 사용 여부 |
| `popbill_corp_num` | text | 팝빌 연동 사업자번호 |
| `popbill_user_id` | text | 팝빌 회원 아이디 |
| `popbill_is_test` | boolean default true | 테스트베드 사용 여부 |

> 팝빌 LinkID / SecretKey는 DB에 저장하지 않고 **환경변수**로 관리한다.

---

## 6. 개발 작업 목록

> 모든 항목이 MVP 범위에 포함된다. Phase는 **구현 순서**를 의미한다.

### Phase 1 — 기반 구축 `[MVP]`

| # | 작업 | 우선순위 |
|---|---|---|
| 1.1 | Next.js + TypeScript + Tailwind + shadcn/ui 프로젝트 초기화 | ★★★ |
| 1.2 | Supabase 프로젝트 연결, `@supabase/ssr` 설정 | ★★★ |
| 1.3 | 전체 테이블 마이그레이션 SQL 작성 및 적용 (5장 전체) | ★★★ |
| 1.4 | RLS 정책 적용 (인증 사용자만 접근) | ★★★ |
| 1.5 | 로그인 화면 + middleware 인증 가드 | ★★★ |
| 1.6 | 공통 레이아웃 (사이드바 / 모바일 하단 탭) | ★★★ |
| 1.7 | 금액 유틸리티 함수 (`lib/money.ts`) — 부가세 분리·검증·포맷 | ★★★ |
| 1.8 | `audit_logs` 자동 기록 헬퍼 구현 | ★★☆ |
| 1.9 | 소프트 삭제 공통 훅 구현 | ★★☆ |

### Phase 2 — 마스터 데이터 `[MVP]`

| # | 작업 | 우선순위 |
|---|---|---|
| 2.1 | 거래처 관리 — 목록/등록/수정/삭제 | ★★★ |
| 2.2 | 거래처 자동완성 컴포넌트 (Combobox) | ★★★ |
| 2.3 | 차량·기사 관리 — 목록/등록/수정/삭제 | ★★★ |
| 2.4 | 차량 자동완성 컴포넌트 (선택 시 기사명·연락처 자동 채움) | ★★★ |
| 2.5 | 사업자등록번호 형식 검증 (10자리 + 체크섬) | ★★☆ |

### Phase 3 — 배차 & 매출 `[MVP · 핵심]`

| # | 작업 | 우선순위 |
|---|---|---|
| 3.1 | 배차 등록 폼 (모바일 우선 레이아웃) | ★★★ |
| 3.2 | 합계금액 입력 → 공급가액·부가세 실시간 자동 분리 표시 | ★★★ |
| 3.3 | 저장 시 부가세 정합성 검증 (불일치 저장 차단) | ★★★ |
| 3.4 | 중복 입력 감지 모달 | ★★☆ |
| 3.5 | 배차/매출 목록 — 기간·거래처·입금여부 필터 | ★★★ |
| 3.6 | 입금 처리 (개별 + 체크박스 일괄 처리, 입금일 입력) | ★★★ |
| 3.7 | 배차 상세/수정/삭제 (이력 로그 기록) | ★★★ |
| 3.8 | 미입금 건 목록 및 배지 표시 | ★★★ |
| 3.9 | 월별 매출 집계 + 검산 경고 배너 | ★★☆ |

### Phase 4 — 지출 관리 `[MVP]`

| # | 작업 | 우선순위 |
|---|---|---|
| 4.1 | 지출 카테고리 상수 정의 (대분류/소분류 연동 셀렉트) | ★★★ |
| 4.2 | 지출 등록 폼 | ★★★ |
| 4.3 | 지출 목록 — 기간·대분류·소분류 필터 | ★★★ |
| 4.4 | 카테고리별 월간 집계 (합계 + 비중) | ★★☆ |
| 4.5 | 지출 수정/삭제 (이력 로그 기록) | ★★★ |

### Phase 5 — 대시보드 `[MVP]`

| # | 작업 | 우선순위 |
|---|---|---|
| 5.1 | 이번 달 매출(공급가액) / 지출 / 순이익 요약 카드 | ★★★ |
| 5.2 | 미입금 건수 및 미수금 총액 카드 (클릭 시 목록 이동) | ★★★ |
| 5.3 | 최근 6개월 매출·지출 추이 차트 | ★★☆ |
| 5.4 | 이번 달 지출 카테고리 구성 차트 | ★☆☆ |
| 5.5 | 최근 등록 배차 5건 리스트 | ★☆☆ |

### Phase 6 — 세금계산서 `[MVP]`

| # | 작업 | 우선순위 |
|---|---|---|
| 6.1 | 계산서 발행 화면 — 미발행 배차 건 선택 → 합산 → 계산서 생성 | ★★★ |
| 6.2 | **수동 기록 모드** (기본값, 무료) — 발행 내역만 저장 | ★★★ |
| 6.3 | **팝빌 발행 모드** — 팝빌 SDK 연동, 테스트베드 우선 | ★★★ |
| 6.4 | 발행 결과 처리 (승인번호 저장 / 실패 시 에러 메시지 저장) | ★★★ |
| 6.5 | 발행 이력 목록 (기간·거래처·상태 필터) | ★★★ |
| 6.6 | 월별 매입 계산서 조회 (팝빌 API) 및 로컬 저장 | ★★☆ |
| 6.7 | 매입 계산서 수동 입력 (팝빌 미사용 시 대체 수단) | ★★☆ |
| 6.8 | 모드 전환 UI — 설정에서 팝빌 사용 On/Off | ★★☆ |

### Phase 7 — 엑셀 입·출력 `[MVP]`

| # | 작업 | 우선순위 |
|---|---|---|
| 7.1 | 매출 엑셀 내보내기 (기간 지정) | ★★★ |
| 7.2 | 지출 엑셀 내보내기 (기간 지정) | ★★★ |
| 7.3 | 월별 종합 리포트 엑셀 (매출/지출/순이익 시트 분리) | ★★☆ |
| 7.4 | **기존 엑셀 데이터 가져오기(Import)** — 템플릿 다운로드 제공 | ★★★ |
| 7.5 | Import 검증 화면 — 오류 행 표시 후 사용자 확인 뒤 반영 | ★★★ |

### Phase 8 — 마감 `[MVP]`

| # | 작업 | 우선순위 |
|---|---|---|
| 8.1 | 설정 화면 (내 사업자 정보, 팝빌 설정) | ★★★ |
| 8.2 | 휴지통 화면 (소프트 삭제 건 복구) | ★★☆ |
| 8.3 | 변경 이력 조회 화면 | ★☆☆ |
| 8.4 | 모바일 반응형 전체 점검 | ★★★ |
| 8.5 | Vercel 배포 및 환경변수 설정 | ★★★ |

---

## 7. 화면별 상세 명세

### 7.1 로그인 `/login`

- 이메일 + 비밀번호 입력, 로그인 버튼
- 회원가입 링크 **없음**
- 실패 시 "이메일 또는 비밀번호가 올바르지 않습니다" (구체적 원인 노출 금지)
- 로그인 성공 → `/` (대시보드)

### 7.2 대시보드 `/`

**상단 요약 카드 (4개)**

| 카드 | 내용 |
|---|---|
| 이번 달 매출 | 공급가액 합계 (전월 대비 증감 %) |
| 이번 달 지출 | 지출 합계 (전월 대비 증감 %) |
| 이번 달 순이익 | 매출 공급가액 − 수수료 − 지출 |
| 미수금 | 미입금 건수 + 총액 (클릭 → 매출 목록 미입금 필터) |

**하단**
- 최근 6개월 매출·지출 추이 (막대 또는 라인 차트)
- 이번 달 지출 카테고리 구성 (도넛 차트)
- 최근 등록 배차 5건

**검산 배너**: 개별 건 합계와 집계 값이 불일치할 경우 상단에 경고 배너 표시

### 7.3 배차/매출 관리 `/dispatches`

**목록**
- 필터: 기간(빠른 선택: 이번 달 / 지난 달 / 직접 지정), 거래처, 입금여부, 지불방법
- 컬럼: 운송일자 · 거래처 · 상차지 → 하차지 · 파렛 · 차량번호(기사명) · 합계운임 · 수수료 · 지불방법 · 입금여부
- 목록 하단 **합계 행**: 건수, 합계운임 합, 공급가액 합, 수수료 합
- 체크박스 다중 선택 → `일괄 입금 처리` (입금일 지정)
- 엑셀 내보내기 버튼
- 모바일: 카드 리스트 (날짜 · 거래처 · 구간 · 금액 · 입금 배지)

**등록/수정 폼 `/dispatches/new`, `/dispatches/[id]/edit`**

입력 순서(모바일 최적화):
1. 운송일자 (기본값: 오늘)
2. 거래처 — 자동완성 (미등록 시 즉시 신규 등록 가능)
3. 상차지 / 하차지 — 텍스트, 최근 입력값 자동완성
4. 파렛 수량 — 숫자
5. 차량 — 자동완성 (선택 시 기사명·연락처 자동 채움, 직접 입력도 허용)
6. 운수사 / 배차자
7. **합계 운임** — 입력 시 아래에 실시간 표시: `공급가액 ○○○원 / 부가세 ○○○원`
8. 면세 여부 체크박스
9. 수수료 (정액)
10. 지불방법 — 세금계산서 / 계좌이체 / 현금
11. 비고

- 저장 버튼은 모바일에서 화면 하단 고정
- 저장 시: 부가세 정합성 검증 → 중복 감지 → 트랜잭션 저장 → 이력 로그 기록

### 7.4 지출 관리 `/expenses`

**목록**
- 필터: 기간, 대분류, 소분류, 증빙 수취 여부
- 컬럼: 지출일자 · 대분류 · 소분류 · 거래처 · 금액 · 결제수단 · 증빙 · 비고
- 하단 합계 행 + 대분류별 소계
- 엑셀 내보내기

**등록 폼**
1. 지출일자 (기본값: 오늘)
2. 대분류 선택 → 소분류 셀렉트 자동 갱신
3. 금액
4. 거래처/상호
5. 결제수단 (카드 / 계좌이체 / 현금)
6. 증빙(계산서) 수취 여부 체크박스
7. 비고

**월간 집계 탭**
- 대분류 → 소분류 트리 형태로 금액·비중 표시
- 전월 대비 증감 표시

### 7.5 거래처 관리 `/clients`

- 목록: 사업자명 · 사업자번호 · 대표자 · 담당자 · 연락처 · 최근 거래일 · 누적 매출
- 검색: 사업자명 / 사업자번호
- 등록·수정 폼: 5.1 필드 전체
- 상세 화면: 해당 거래처의 배차 이력 및 미수금 현황
- 삭제: 배차 이력이 있는 경우 소프트 삭제만 허용하고 경고 표시

### 7.6 차량·기사 관리 `/vehicles`

- 목록: 차량번호 · 기사명 · 연락처 · 운수사 · 차종 · 최근 배차일 · 배차 건수
- 검색: 차량번호 / 기사명
- 등록·수정 폼: 5.2 필드 전체
- 차량번호 중복 등록 차단

### 7.7 세금계산서 `/tax-invoices`

**탭 구성: `발행(매출)` / `매입 조회`**

#### 발행 탭
- 상단: `새 계산서 작성` 버튼
- 목록: 작성일자 · 거래처 · 공급가액 · 부가세 · 합계 · 모드(수동/팝빌) · 상태 배지
- 상태 배지: 기록됨(회색) / 발행완료(파랑) / 전송완료(초록) / 실패(빨강) / 취소(회색)

**새 계산서 작성 흐름**
1. 거래처 선택
2. 해당 거래처의 **계산서 미발행 배차 건 목록** 표시 → 체크박스로 선택
3. 선택 건의 공급가액·부가세·합계 자동 합산 표시 (수동 수정 가능하되 수정 시 경고)
4. 품목명 입력 (기본값 `운송용역`)
5. **발행 방식 선택**
   - `수동 기록` (기본값): 저장만 수행, 상태 `RECORDED`. 홈택스에서 별도 발행하도록 안내 문구 표시.
   - `팝빌 발행`: 설정에서 팝빌 사용이 켜져 있을 때만 활성화. 발행 시 확인 모달(과금 안내 포함) → API 호출 → 결과에 따라 상태 및 승인번호 저장.
6. 실패 시 에러 메시지를 저장하고 재시도 버튼 제공

#### 매입 조회 탭
- 조회 월 선택 → `팝빌로 조회` 버튼 (팝빌 사용 시)
- 결과 목록: 작성일자 · 공급자 상호 · 사업자번호 · 공급가액 · 부가세 · 합계 · 승인번호
- 하단 합계 (매입세액 총액)
- `수동 입력` 버튼 — 팝빌 미사용 시 직접 등록
- 엑셀 내보내기

### 7.8 엑셀 가져오기 `/import`

1. 대상 선택: 매출(배차) / 지출
2. **템플릿 엑셀 다운로드** 버튼 (컬럼 헤더가 정확히 정의된 양식)
3. 파일 업로드
4. **검증 결과 화면**
   - 정상 행 / 오류 행을 구분하여 표시
   - 오류 유형: 필수값 누락, 금액 형식 오류, 날짜 형식 오류, 미등록 거래처, 중복 의심
   - 오류 행은 화면에서 직접 수정 가능
   - 미등록 거래처·차량은 `자동 생성` 옵션 제공
5. `○○건 반영하기` 버튼 → 트랜잭션 일괄 저장
6. 결과 요약 표시 (성공 n건 / 실패 n건)

> Import된 데이터는 `audit_logs`에 `action='CREATE', source='IMPORT'`로 기록한다.

### 7.9 설정 `/settings`

- **내 사업자 정보**: 사업자명, 사업자등록번호, 대표자명, 주소, 업태, 종목
- **팝빌 연동**: 사용 여부 토글, 연동 사업자번호, 회원 아이디, 테스트베드 여부
  - 연동 상태 확인 버튼 (팝빌 잔여 포인트 조회로 연결 검증)
  - 팝빌 사용 시 건당 과금 발생 안내 문구 상시 표시
- **데이터 관리**: 전체 데이터 백업(엑셀) 다운로드, 휴지통 이동

### 7.10 휴지통 `/trash`

- 삭제된 배차/지출/거래처/차량 목록 (삭제일 표시)
- `복구` 버튼 → `deleted_at = NULL`, 이력 로그 `RESTORE` 기록
- 30일 경과 건은 안내 문구 표시 (자동 영구삭제는 구현하지 않음)

---

## 8. 제약 사항 및 비범위

### 8.1 제약 사항

| 항목 | 내용 |
|---|---|
| 사용자 | 관리자 1인 전용. 권한 관리·다중 사용자 미지원 |
| 홈택스 직접 연동 | **불가**. 홈택스는 공개 API를 제공하지 않으며, 크롤링·인증서 자동화 방식은 채택하지 않는다. 세금계산서 발행은 팝빌 API 또는 수동 기록으로만 처리한다. |
| 팝빌 과금 | 발행 건당 요금 발생. 따라서 **수동 기록 모드를 기본값**으로 하고, 팝빌 발행은 명시적 확인 후에만 실행한다. 개발·검증은 팝빌 테스트베드에서 수행한다. |
| 데이터 정확성 | 전자장부 수준의 정확성 요구. `4.3 금액·정확성 규칙` 전 항목 필수 준수. |
| 기존 데이터 | 엑셀 기반 과거 데이터 이관 필요 (Phase 7) |
| 비용 | 인프라 비용 0원 목표 (Supabase·Vercel 무료 티어) |
| 언어·통화 | 한국어 단일, KRW 단일 |
| 브라우저 | 최신 Chrome / Safari (iOS) 기준 |

### 8.2 비범위 (이번 개발에 포함하지 않음)

- 화주·기사용 별도 계정 및 외부 사용자 로그인
- 실시간 배차 매칭, GPS 차량 위치 추적
- 카드사·은행 자동 연동(스크래핑)을 통한 지출 자동 등록
- 전자계약서, 인수증(운송장) 발행
- 알림(문자·카카오톡·이메일) 발송
- 다국어, 다중 통화

---

## 9. 완료 기준 (Acceptance Criteria)

- [ ] 로그인 없이 어떤 데이터 화면에도 접근할 수 없다.
- [ ] 배차 1건 등록 시 매출 데이터가 자동 생성되고, 부가세가 정확히 분리된다. (`공급가액 + 부가세 = 합계금액` 항상 성립)
- [ ] 임의의 합계금액 100건에 대해 `공급가액 + 부가세 = 합계금액` 검증이 100% 통과한다.
- [ ] 삭제한 데이터는 휴지통에서 복구할 수 있다.
- [ ] 모든 생성·수정·삭제 작업이 변경 이력에 남는다.
- [ ] 대시보드의 월 합계가 목록의 개별 건 합계와 일치한다.
- [ ] 미입금 건이 대시보드와 목록에서 즉시 식별된다.
- [ ] 매출·지출 데이터를 엑셀로 내보낼 수 있다.
- [ ] 템플릿 엑셀로 기존 데이터를 가져올 수 있고, 오류 행이 사전에 표시된다.
- [ ] 세금계산서를 수동 기록 모드로 저장할 수 있다.
- [ ] 팝빌 테스트베드에서 세금계산서 발행이 성공하고 승인번호가 저장된다.
- [ ] 월별 매입 계산서를 조회하여 목록으로 확인할 수 있다.
- [ ] 모바일(390px 폭)에서 배차 등록이 스크롤 없이 편하게 완료된다.

---

## 10. 환경변수

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# 팝빌 (Popbill)
POPBILL_LINK_ID=
POPBILL_SECRET_KEY=
POPBILL_CORP_NUM=
POPBILL_USER_ID=
POPBILL_IS_TEST=true
```

---

*문서 버전 1.0 · 작성일 2026-07-30*
